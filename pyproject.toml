[project]
name = "youtube-video-and-playlist-downloader"
version = "0.1.0"
description = "Python scripts for downloading YouTube videos and playlists with pytubefix."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "imageio-ffmpeg==0.6.0",
    "pytubefix==10.10.1",
]
